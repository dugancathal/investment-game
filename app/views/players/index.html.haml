- model_class = Player
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table
  %thead
    %tr
      %th= model_class.human_attribute_name(:email)
  %tbody
    - @players.each do |player|
      %tr
        %td= link_to player.email, player
